/**
 * Components Index
 * Export reusable component objects from a single entry point.
 * Add component page objects here as the test suite grows.
 *
 * Current components:
 * - NavigationBar
 */

export { NavigationBar } from './NavigationBar.js';
